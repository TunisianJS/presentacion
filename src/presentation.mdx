import uid from 'cross-uid';
import { Head } from 'mdx-deck';
import Box from './Box';
import Logo from './Logo';
import P from './Paragraph';
import Page from './Page';
import Profile from './Profile';
import Title from './Title';
import SubmitTalk from './SubmitTalk'
import { Kahoot, Stickers } from './ConditionalRendering'

import { slide1, slide2, slide3, slide4, slide5, slide6, slide7, slide8, slide9 }  from './data.json'

<Head>
  <link href="https://fonts.googleapis.com/css?family=Libre+Franklin:400,700" rel="stylesheet" />
</Head>

<Page confetti>
  <Title text={slide1.title} />
  <br/><br/>
  <Logo {...slide1.logo} />
</Page>

---

<Page>
  <Title text={slide2.title} />
  <div
    style={{
      display: 'flex',
      flexWrap: 'wrap',
      justifyContent: 'center',
    }}
  >
  { 
    slide2.profiles.map(({ name, avatar, github }) => (
      <Profile key={uid()} github={github} avatar={avatar} name={name} />
    ))
  }
  </div>
</Page>

---

<Page>
  <Title text={slide3.title} />
    { 
      slide3.boxsText.map(text => (
        <Box key={uid()} text={text} />
      ))
    }
</Page>

---

<Page>
  <Title text={slide4.title} />
    { 
      slide4.boxs.map(({image, text}) => (
        <Box key={uid()} image={image} text={text} />
      ))
    }
</Page>

---

<Page>
  <Title text={slide5.title} />
  
    { 
      slide5.talks.map((talk) => (
        <P key={uid()}>
          <strong>{talk.title}</strong>
          <span> By {talk.speaker}</span>
        </P>
      ))
    }

  <Kahoot value={slide5.kahoot} />
  <Stickers value={slide5.stickers} />

  <P>
    <span style={{ fontSize: '5rem' }}>🚀</span>
  </P>
</Page>

---

<Page>
  <Title text={slide6.title} />
  <SubmitTalk to={slide6.to} />
</Page>

---

<Page>
  <Title text={slide7.title} />
  {
    slide7.sponsors.map((sp) => (
      <P key={uid()}>
        To <strong>{sp.name}</strong> {sp.text}
      </P>
    ))
  }
</Page>

---

<Page>
  <Title text={slide8.title} />
  <div         
    style={{
      display: 'flex',
      flexWrap: 'wrap',
      justifyContent: 'center'
    }} 
  >
  {
    slide8.sponsors.map((sp) => (
      <div
        key={uid()}
        style={{
          flexDirection: 'column',
          padding: '1.5rem',
        }} 
      >
        <img src={sp.image} style={{ width: '200px' }} />
      </div>
    ))
  }</div>
  
</Page>

---

<Page>
  <Logo {...slide9.logo} />
</Page>
